from application.models import Product


class ProductController:

    # Create
    def new_product(self, receiptProductInstance, receiptInstance):
        # name = json.
        # price = json.
        # qty = json.
        # receiptProductInstance(qty=qty)
        # product = Product(name=name, price=price,ReceiptProducts=)
        return ()
# Read
# Update
# Delete
